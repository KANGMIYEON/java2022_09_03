package day09;

public class Exception05 {

	public static void main(String[] args) {
		// 받은 배열 출력. try~catch문 활용하여 예외 잡기. 메세지는 get.Messege 사용.
		
		createRandomArry(arr, 5, 10, 1);
		
//		try {
//			(size<0);
//			}
//		} catch (Exception e) {
//			System.out.println("배열의 길이가 0보다 작습니다.");
//		}
	}

	/*
	 * 기능 : 배열의 길이(size)가 주어지면 길이만큼 배열을 생성해서 값을 돌려주는 메서드
	 * 값은 랜덤으로 생성해서 반환 (max, min)
	 * 매개변수 : 
	 * 리턴타입 : 배열[]
	 * 메서드명 : createRandomArry
	 * size의 값이 0보다 작다면 예외발생 => "배열의 길이가 0보다 작습니다."
	 * */
	
	public static int[] createRandomArry(int arr[], int size, int max, int min) {
		
		arr[i] = new arr[size];
		for(int i=0; i<size; i++) {
			arr[i] = (int)(Math.random()*max)+min;
		}
		return arr;
	}
}
